# Xavier Voice Browser Daemon - Python Dependencies

# Primary STT: faster-whisper (CTranslate2 backend; CPU + int8, no PyTorch)
faster-whisper>=1.0.0

# Voice activity detection: Silero VAD model run via onnxruntime (no PyTorch).
# The silero_vad.onnx model is downloaded at setup (see README), not pip-installed.
onnxruntime>=1.16.0

# Fallback STT (offline, lightweight)
vosk>=0.3.45

# Audio capture (cross-platform)
sounddevice>=0.4.6

# Audio processing
numpy>=1.24.0

# Tests
pytest>=7.0.0
